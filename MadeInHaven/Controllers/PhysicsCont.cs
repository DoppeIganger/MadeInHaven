using System;
using System.Collections.Generic;
using System.Text;
using MadeInHaven.Entites;

namespace MadeInHaven.Controllers
{
    public static class PhysicsCont
    {
        public static void IsCollide(Entity entity)
        {
            //for (int j = entity.posX / Mapcontroller.cellSize; j < (entity.posX + Mapcontroller.cellSize) / Mapcontroller.cellSize; j++)
            //{
            //    for (int i = entity.posY / Mapcontroller.cellSize; i < (entity.posY + Mapcontroller.cellSize) / Mapcontroller.cellSize; i++)
            //    {
            //        if (Mapcontroller.map[i, j] != 0 && Mapcontroller.map[i, j] != 10 && Mapcontroller.map[i, j] != 20 && Mapcontroller.map[i, j] != -1)
            //        {
            //            if (entity.dirY > 0)
            //            {
            //                entity.posY -= 10;
            //            }
            //            if (entity.dirY < 0)
            //            {
            //                entity.posY += 10;
            //            }
            //            if (entity.dirX > 0)
            //            {
            //                entity.posY -= 10;
            //            }
            //            if (entity.dirX < 0)
            //            {
            //                entity.posY += 10;
            //            }
            //        }
            //    }
            //}
        }
    }
}
        
